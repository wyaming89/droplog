# 点滴健康 - React 前端

记录每日健康 - 基于 React + TypeScript + Tailwind CSS 的现代化健康记录应用。

## 技术栈

- **React 18** - UI 框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具
- **Tailwind CSS** - 样式框架
- **Framer Motion** - 动画库
- **Recharts** - 图表库
- **React Router** - 路由管理
- **Lucide React** - 图标库

## 特性

- 🎨 **现代化暗色主题** - 优雅的深色 UI 设计
- 📱 **移动端优先** - 响应式布局，适配手机屏幕
- 🎯 **底部导航** - 首页/趋势/我的 三个主要页面
- 📊 **渐变色卡片** - 不同指标使用不同的渐变色主题
- ⌨️ **数字键盘输入** - 模态框形式的数据录入
- 📈 **数据可视化** - 使用 Recharts 展示趋势图表
- 🔐 **用户认证** - 支持登录/注册/游客模式
- 💾 **本地存储** - 游客模式下数据保存在本地

## 目录结构

```
frontend/
├── src/
│   ├── components/       # 通用组件
│   │   ├── ui/          # 基础 UI 组件
│   │   ├── DataEntryModal.tsx  # 数据录入弹窗
│   │   └── HealthCard.tsx      # 健康指标卡片
│   ├── hooks/           # 自定义 Hooks
│   │   ├── useAuth.ts   # 认证状态管理
│   │   └── useHealthData.ts  # 健康数据管理
│   ├── pages/           # 页面组件
│   │   ├── HomePage.tsx      # 首页
│   │   ├── TrendPage.tsx     # 趋势页
│   │   ├── SettingsPage.tsx  # 设置页
│   │   └── LoginPage.tsx     # 登录页
│   ├── services/        # API 服务
│   │   └── api.ts
│   ├── types/           # TypeScript 类型定义
│   ├── App.tsx          # 根组件
│   ├── main.tsx         # 入口文件
│   └── index.css        # 全局样式
├── package.json
├── vite.config.ts
├── tailwind.config.js
└── tsconfig.json
```

## 开发

```bash
# 安装依赖
npm install

# 开发模式（端口 3000，代理 API 到 8000）
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

## 与后端集成

开发模式下，Vite 会将 `/api` 请求代理到 `http://localhost:8000`。

生产环境下，构建输出到 `dist/` 目录，由 Express 后端直接提供静态文件服务。

## 从根目录运行

```bash
# 安装前端依赖
npm run install:frontend

# 构建前端
npm run build:frontend

# 前端开发模式
npm run dev:frontend

# 启动后端（会自动使用构建好的前端）
npm start
```
